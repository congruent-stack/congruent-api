{
  "name": "@congruent-stack/monorepo",
  "version": "0.1.8",
  "description": "Typescript schema-first tooling for agnostic REST APIs.",
  "keywords": [],
  "author": "congruent-stack",
  "license": "MIT",
  "packageManager": "pnpm@10.15.1",
  "files": [],
  "devDependencies": {
    "vitest": "catalog:"
  },
  "scripts": {
    "pkgs:version:bump": "pnpm --filter \"./packages/**\" exec pnpm version $npm_config_bump_or_version",
    "root:version:bump": "pnpm exec pnpm version $npm_config_bump_or_version",
    "version:bump": "echo $npm_config_bump_or_version && pnpm run root:version:bump && pnpm run pkgs:version:bump"
  }
}
